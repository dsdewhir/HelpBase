<!DOCTYPE html>
<html>
<head>
  <title>HelpBase Online</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <link href='http://fonts.googleapis.com/css?family=Montserrat+Alternates:700' rel='stylesheet' type='text/css'>
</head>
<body>
	<% flash.each do |name, msg| %>
    	<%= content_tag :div, msg, :id => "flash#{name}", :style => "position:absolute;z-index:100;" %>
  	<% end %>
  	<div class="login_nav" style="position:absolute;z-index:100;">
  		<% if current_user%>
  			Logged in as <%= current_user.email%>.&nbsp; <%= link_to "Log Out",log_out_path%>
  		<%else%>
  			<%= link_to "Sign up", sign_up_path %> or 
    		<%= link_to "Log in", log_in_path %>
  		<%end%>
  	</div>

<%= yield %>

</body>
</html>
